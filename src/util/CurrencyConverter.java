package util;

public class CurrencyConverter {

    // Metodo que realiza a conversão (valor * taxa)
    public static double converter(double valor, double taxa) {
        return valor * taxa;
    }
}
